# Week 03 素材清单

- `fig03-01_thread_coverage`：`n=1000`、`blockDim=256` 时四个 block 的覆盖范围。
- `fig03-02_tensor_to_kernel`：Python wrapper、C++ binding、CUDA launcher、kernel 四层调用链。
- `fig03-03_async_timeline`：CPU enqueue、GPU execute 与同步点的时间线。
- `table03-01_memory_hierarchy`：register、shared memory、global memory 的作用域和用途。

样章当前使用内嵌 Mermaid 表达图意。风格确认后从同一图意导出课件素材。

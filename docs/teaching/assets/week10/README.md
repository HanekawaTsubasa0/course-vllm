# Week 10 素材清单

- `fig10-01_contiguous_reservation`：按最大长度预留导致的空间浪费。
- `fig10-02_address_translation`：逻辑 position 6 映射到物理 slot 10。
- `fig10-03_two_sequence_pool`：两条 sequence 的 block table 共同指向物理块池。
- `fig10-04_refcount_lifecycle`：共享前缀块从复用到释放的状态变化。
- `table10-01_block_size_tradeoff`：不同 block size 的内部碎片与元数据代价。

样章当前使用内嵌 Mermaid 表达图意。风格确认后从同一图意导出课件素材。

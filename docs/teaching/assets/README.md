# 讲义与 PPT 共用素材

素材按周次保存。命名采用 `figXX-YY_topic` 或 `tableXX-YY_topic`，其中 `XX` 是章号，`YY` 是章内顺序。

每项素材应保留可编辑源文件，并在需要时导出 SVG 或 PNG。正文、讲课 PPT 和教师备注引用同一份素材，不分别维护内容不同的副本。

当前已建立：

- `week03/`：CUDA 执行层次、Tensor 到 kernel 调用链、线程覆盖图。
- `week10/`：逻辑块映射、物理块池、引用计数状态图、碎片比较图。
